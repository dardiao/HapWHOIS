diff --git a/crates/hapcli-egui-app/src/theme.rs b/crates/hapcli-egui-app/src/theme.rs
--- a/crates/hapcli-egui-app/src/theme.rs
+++ b/crates/hapcli-egui-app/src/theme.rs
@@ -9,7 +9,7 @@ fn hex(h: u32) -> Color32 {
     Color32::from_rgb((h >> 16) as u8, (h >> 8) as u8, h as u8)
 }
 
-fn widget(bg: u32, weak: u32, fg: u32, border: u32, rounding: f32) -> egui::WidgetVisuals {
-    egui::WidgetVisuals {
+fn widget(bg: u32, weak: u32, fg: u32, border: u32, rounding: f32) -> egui::style::WidgetVisuals {
+    egui::style::WidgetVisuals {
         bg_fill: hex(bg),
         weak_bg_fill: hex(weak),
         bg_stroke: Stroke::new(1.0, hex(border)),
@@ -33,7 +33,7 @@ fn dark_visuals() -> egui::Visuals {
     v.window_stroke = Stroke::new(1.0, hex(0x2a323c));
     v.menu_rounding = Rounding::same(8.0);
-    v.selection = egui::Selection {
+    v.selection = egui::style::Selection {
         bg_fill: hex(0x2c4a6e),
         stroke: Stroke::new(1.0, hex(0x4f86c6)),
     };
-    v.widgets = egui::Widgets {
+    v.widgets = egui::style::Widgets {
         noninteractive: widget(0x1d232b, 0x1d232b, 0xb9c0ca, 0x242c36, 6.0),
         inactive: widget(0x242c36, 0x242c36, 0xd5dae1, 0x303a46, 6.0),
         hovered: widget(0x2d3a49, 0x2d3a49, 0xffffff, 0x3d4b5d, 6.0),
@@ -60,7 +60,7 @@ fn light_visuals() -> egui::Visuals {
     v.window_stroke = Stroke::new(1.0, hex(0xd8dce2));
     v.menu_rounding = Rounding::same(8.0);
-    v.selection = egui::Selection {
+    v.selection = egui::style::Selection {
         bg_fill: hex(0xcfe3ff),
         stroke: Stroke::new(1.0, hex(0x4a90e2)),
     };
-    v.widgets = egui::Widgets {
+    v.widgets = egui::style::Widgets {
         noninteractive: widget(0xffffff, 0xffffff, 0x3c4149, 0xe2e5ea, 6.0),
         inactive: widget(0xeef1f5, 0xeef1f5, 0x1c1e21, 0xd8dce2, 6.0),
         hovered: widget(0xe2e8f0, 0xe2e8f0, 0x111111, 0xc9d2dc, 6.0),
@@ -82,7 +82,7 @@ pub fn apply_egui_theme(ctx: &egui::Context, choice: ThemeChoice) {
     };
     ctx.set_visuals(visuals);
 
-    let mut style: egui::Style = (**ctx.style()).clone();
+    let mut style: egui::Style = ctx.style().clone();
     style.spacing.item_spacing = Vec2::new(8.0, 6.0);
     style.spacing.button_padding = Vec2::new(12.0, 6.0);
     style.spacing.menu_margin = Margin::same(8.0);
